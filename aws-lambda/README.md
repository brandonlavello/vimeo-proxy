Vimeo Livestream Proxy (AWS Lambda)

Environment Variables:
- VIMEO_TOKEN
- LIVE_EVENT_ID

Endpoint:
- Lambda Function URL

Notes:
- Returns 302 redirect to Vimeo HLS URL
- Includes basic in-memory caching
